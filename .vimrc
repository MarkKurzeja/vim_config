" Because each .vimrc is different in how the settings work, it is better
" to just contain all of my commands in a seperate file so that I can
" source them when I need them and not need to worry about version control
" messing up my actual VIMRC code when it conflicts with work settings

source ~/.marks_vimrc
